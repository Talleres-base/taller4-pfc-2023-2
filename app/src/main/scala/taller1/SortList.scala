package taller1

/**
 * Created by Carlos Delgado
 * Class for the second, third and four points of the assignment one of the course of functional programming
 */
class SortList {

  /**
   * Method to return the values of a list l lower than a value v
   * @param l list of integers
   * @param v value to compare
   * @return list of integers lower than v
   */
  def menoresQue(l:List[Int], v:Int) : List[Int] = {
    //Replace code
    List()
  }

  /**
   * Method to return the values of a list l greater than a value v
   * @param l list of integers
   * @param v value to compare
   * @return list of integers greater than v
   */
  def mayoresQue(l:List[Int], v:Int) : List[Int] = {
    //Replace code
    List()
  }

  /**
   * Method to return the k-th value of a non-empty list l
   * @param l list of integers
   * @param k position of the value to return if the list is ordered
   */
  def k_elemento(l:List[Int], k:Int) : Int = {
    //Replace code
    0
  }

  /**
   * Sort a list of integers using the past methods
   * @param l list of integers
   * @return sorted list
   */
  def ordenar(l:List[Int]) : List[Int] = {
    //Replace code
    List()
  }

}
